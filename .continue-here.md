---
phase: ad-hoc
task: 5
total_tasks: 6
status: in_progress
last_updated: 2026-03-26T16:27:36-03:00
---

<current_state>
Fundacao local pronta e verificada. O repo agora tem lint, typecheck, test e build na raiz, stack Docker com Postgres/Redis/MinIO/Mailpit e uma API NestJS em `apps/api` com auth, dreams, proposals e accept proposal funcionando por teste de integracao.
</current_state>

<completed_work>

- Task 1: Auditoria do frontend e localizacao dos mocks principais - Done
- Task 2: Fundacao do repo com `.env.example`, `docker-compose.yml`, ADR, TS, ESLint e Vitest - Done
- Task 3: API NestJS criada em `apps/api` com `health`, `register` e `login` - Done
- Task 4: Dominio core implementado com `POST /dreams`, `GET /dreams/public`, `POST /dreams/:dreamId/proposals` e `POST /proposals/:proposalId/accept` - Done
- Task 6: Verificacao executada com sucesso (`lint`, `typecheck`, `test`, `build`, `docker compose config -q`, `docker compose up -d`) - Done
</completed_work>

<remaining_work>

- Task 5: Integrar o frontend aos endpoints reais e remover os mocks correspondentes
- Trocar `src/app/context/AppContext.tsx` de role demo para sessao real com token persistido
- Criar cliente HTTP central em `src/app/lib/api.ts`
- Conectar pelo menos `Login`, `Register`, `CreateDream` e `SupporterDreamDetail` ao backend
- Depois disso, avancar para remover `mockData.ts` desses fluxos antes de atacar dashboards/chat/admin completos
</remaining_work>

<decisions_made>

- Mantido o frontend na raiz; a API entrou em `apps/api` para reduzir churn estrutural
- NestJS escolhido para a API; TypeORM usado para acelerar entrega sem depender de gerador externo
- Testes da API usam `sqljs` em memoria; ambiente local real usa Postgres via Docker
- Validacao local exigida pelo projeto foi tratada como gate obrigatorio antes de seguir
</decisions_made>

<blockers>
- Nao ha bloqueio tecnico imediato
- Este diretorio nao e um repositório Git proprio; nao houve commit de WIP
</blockers>

<context>
O backend ja passou no fluxo real de cadastro, login, criacao de sonho, envio de proposta e aceite com abertura de conversa. O frontend ainda esta em modo prototipo e e a proxima frente. O melhor proximo passo e nao tentar trocar tudo de uma vez: primeiro autenticar de verdade e mover os fluxos core para API; depois substituir dashboards/chat/admin.
</context>

<next_action>
Start with: criar `src/app/lib/api.ts` e refatorar `src/app/context/AppContext.tsx` para armazenar `accessToken`, `refreshToken` e `currentUser` reais em `localStorage`, removendo a dependencia de usuarios mockados e do `DemoBar`.
</next_action>
