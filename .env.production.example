APP_ENV=production
APP_URL=https://nextdream.ong.br
API_BIND_ADDRESS=127.0.0.1
API_PORT=4000
NODE_ENV=production
WEB_BIND_ADDRESS=127.0.0.1

# Frontend in production should call API through reverse proxy (/api)
VITE_API_URL=/api
VITE_APP_ENV=production
VITE_GA_MEASUREMENT_ID=G-P7HEP47P2M
VITE_SANDBOX_HOSTNAME=sandbox.nextdream.ong.br
VITE_SENTRY_DSN=<PROD_SENTRY_DSN_WEB>
VITE_SENTRY_RELEASE=<GIT_SHA_OR_RELEASE_TAG>
VITE_SENTRY_TRACES_SAMPLE_RATE=0.15

# Comma-separated allowed origins for browser access
CORS_ORIGIN=https://nextdream.ong.br,https://www.nextdream.ong.br,https://sandbox.nextdream.ong.br
THROTTLE_TTL_MS=60000
THROTTLE_LIMIT=120
# Tight limit for repeated authentication attempts.
LOGIN_THROTTLE_TTL_MS=60000
LOGIN_THROTTLE_LIMIT=5
# Production is expected to run behind a trusted reverse proxy.
TRUST_PROXY=true
# Keep this in sync with docker-compose.prod.yml network settings.
DOCKER_SUBNET=172.30.0.0/24
# Fixed IP for the API container inside DOCKER_SUBNET.
API_CONTAINER_IP=172.30.0.3
# Fixed IP for the web reverse-proxy container inside DOCKER_SUBNET.
WEB_PROXY_IP=172.30.0.2
# Required when TRUST_PROXY=true: comma-separated trusted proxy IPs/CIDRs.
# Include only real reverse-proxy IPs (for example load balancer/static proxy IPs).
PROXY_TRUSTED_IPS=127.0.0.1/32,172.30.0.2/32
# Abort deployment if the host has less free disk than this threshold after light cleanup.
MIN_FREE_DISK_MB=2048

# Production database is external (not provisioned by docker-compose.prod.yml)
# GitHub deploy mapping:
#   PROD_DATABASE_URL -> DATABASE_URL
#   PROD_JWT_ACCESS_SECRET -> JWT_ACCESS_SECRET
#   PROD_JWT_REFRESH_SECRET -> JWT_REFRESH_SECRET
#   RESEND_API_KEY -> RESEND_API_KEY
#   RESEND_FROM_EMAIL -> RESEND_FROM_EMAIL
#   SENTRY_DSN_WEB -> SENTRY_DSN_WEB
#   SENTRY_DSN_API -> SENTRY_DSN
DATABASE_URL=postgresql://postgres.<SUPABASE_PROJECT_REF>:<YOUR_SUPABASE_PASSWORD>@aws-1-sa-east-1.pooler.supabase.com:6543/postgres?sslmode=require
JWT_ACCESS_SECRET=<PROD_JWT_ACCESS_SECRET>
JWT_REFRESH_SECRET=<PROD_JWT_REFRESH_SECRET>
RESEND_API_KEY=<PROD_RESEND_API_KEY>
RESEND_FROM_EMAIL=<PROD_RESEND_FROM_EMAIL>
JWT_ACCESS_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d
SENTRY_DSN=<PROD_SENTRY_DSN_API>
SENTRY_DSN_WEB=<PROD_SENTRY_DSN_WEB>
SENTRY_ENVIRONMENT=production
SENTRY_TRACES_SAMPLE_RATE=0.15
SENTRY_RELEASE=<GIT_SHA_OR_RELEASE_TAG>
AUTH_COOKIE_DOMAIN=nextdream.ong.br
AUTH_COOKIE_SECURE=true
SENTRY_EMIT_STARTUP_TEST_LOG=false

# Sandbox production activation is hostname-based.
# Keep VITE_APP_ENV=sandbox only for localhost and automated tests.
# Sandbox deploys should override:
#   APP_ENV=sandbox
#   APP_URL=https://sandbox.nextdream.ong.br
#   AUTH_COOKIE_DOMAIN=
